# 俱乐部开源实习课题任务书

## 一、课题基本信息

- **课题名称**：AtomGit Mobile Android 客户端的设计与实现
- **指导导师**：慕冬亮
- **开发语言**：Kotlin；UI 使用 Jetpack Compose
- **预计项目时长**：12 周，项目合计 240–320 人时，以 280 人时为排期基线；领取课题后 3 个月内完成开发与验收
- **团队规模**：2 名同学协作开发
- **投入说明**：两人平均分担时，每人每周约投入 10–14 小时；前两周根据团队经验与接口验证结果复核投入
- **难度等级**：高级
- **版本目标**：交付以了解项目动态和处理当前用户 AtomGit 待办为核心、能够安装和使用的首版移动工作台；第 2 周确定验收范围，第 10 周完成首版功能，第 11–12 周用于测试、修复、文档与验收
- **课题背景信息介绍**：

AtomGit（atomgit.com）提供仓库托管、Issue 跟踪、合并请求（Pull Request）、流水线等开发协作能力。本课题以现有 AtomGit 工作台的任务组织方式为主要参考，以 OpenAPI 为数据来源，建设面向当前用户的 Android 移动工作台。应用以动态为默认首页，帮助用户了解本人及选定项目的近期活动，并通过待办发现需要自己处理的事项、进入上下文、完成评论或评审并确认结果；仓库、文件和流水线用于支撑待办处理，也可从“我的项目”“我的组织”进入浏览。GitHub Mobile 仅作为移动端细节交互的辅助参考。

平台资源接口以 `https://api.atomgit.com/api/v5` 为主，Actions 接口使用 `https://api.atomgit.com/api/v8`。具体路径、认证、权限、分页和响应字段必须逐端点验证，不能仅凭 API 版本或其他客户端的实现推断。

首版以原生 Android 应用直接访问已验证的接口为基本方案。OAuth 若依赖保密的服务端凭据，或后续需要实时推送服务，应另行设计服务端并重新评估范围，不能默认包含在本课题工时内。

## 二、技能要求

- **具有 Android App 开发经验者优先，有 Kotlin 或 Jetpack Compose 项目实践者更佳；不将既有项目经验作为报名硬性条件。**
- 具备编程基础，能够使用或在项目初期掌握 Kotlin、Android Studio、Gradle 和 Android 基本生命周期。
- 熟悉 Jetpack Compose、Material 3、状态提升、深色模式和基本自适应布局。
- 理解分层架构、单向数据流（UDF）、单一事实来源（SSOT），掌握 ViewModel、StateFlow、Coroutines 和生命周期感知的状态收集。
- 了解 Repository、按需 UseCase 和构造函数依赖注入，能够使用 Hilt 管理依赖。
- 具备 REST API 对接能力，能够使用 Retrofit/OkHttp 处理认证、分页、错误、取消与受控重试。
- 理解 PAT、OAuth 授权码、PKCE、浏览器回调和 Android Keystore 的作用及安全边界。
- 理解 Git 仓库、分支、提交、Issue、PR 和代码评审的基本语义。
- 能够编写单元测试、接口契约测试、Compose UI 测试和开发文档。

## 三、课题任务

### 3.1 技术选型与架构

采用 Android 官方推荐的分层架构与单向数据流，基础技术栈为 Kotlin、Jetpack Compose、Material 3、ViewModel、Coroutines/Flow、Hilt、Retrofit/OkHttp、Room 和 Coil。

- **UI 层**：Compose 负责展示和收集用户操作；屏幕级 ViewModel 暴露只读 `StateFlow<UiState>`；页面使用 `collectAsStateWithLifecycle()` 收集状态，通过方法调用向 ViewModel 提交操作。
- **数据层**：Repository 统一协调 API 和本地数据源，封装平台差异、数据映射与缓存策略。Compose 和 ViewModel 不直接访问 Retrofit Service 或 Room DAO。
- **可选领域层**：仅为复杂业务逻辑或跨 ViewModel 复用的逻辑增加 UseCase，例如组合 Issue 与 PR 待办。不要求为每个 API 创建只做转发的 UseCase。
- **数据流**：用户操作 → ViewModel → Repository／可选 UseCase → 数据源；处理结果转为状态，经 ViewModel 驱动界面更新。对每类数据明确权威来源、缓存有效期和写入后刷新规则。
- **导航**：采用单 Activity，底部仅设“动态、待办、通知、我的”四个入口，登录后默认进入动态首页。项目、组织、仓库与事项详情作为二级及更深层页面；从动态、待办或通知进入详情后返回原列表并保留筛选与滚动位置。优先评估 Navigation 3；第 2 周固定兼容的稳定版本与导航方案。若因依赖或团队经验选择 Navigation Compose，应记录理由并统一实现，不混用两套导航栈。
- **工程组织**：首版优先按功能和职责划分包；仅在复用、隔离或构建需求明确时拆分 Gradle 模块。
- **本地存储**：Room 首先用于仓库摘要、待办摘要等需要持久化的数据；设置使用 DataStore。文件、diff、评论和流水线详情按需加载，不要求全部持久化。缓存必须有容量、过期和退出清理策略。
- **凭据存储**：Android Keystore 管理加密密钥，使用标准加密 API（如 AES-GCM）加密 Token 后保存密文；凭据密文排除备份。Keystore 密钥失效时安全清理并要求重新登录，不以明文存储替代。新项目不将已弃用的 EncryptedSharedPreferences 作为必选实现。

### 3.2 信息架构

以现有 AtomGit 工作台的任务分类、状态展示和处理路径为主要参考，按移动端屏幕重新组织，具体视觉遵循 Material 3。底部按 **动态、待办、通知、我的** 顺序设置 4 个入口，动态排在首位，是登录后的默认首页；待办承担具体事项处理。

1. **动态（默认首页）**：展示本人及选定项目的近期活动，帮助用户了解协作进展。首版支持来源展示、时间排序、分页、下拉刷新及关联事项跳转；具体事件类型以接口验证结果为准。动态用于了解发生了什么，需要自己处理的事项集中在待办，需阅读和标记已读的消息集中在通知，不混用三者的状态或数量。
2. **待办**：突出“需要我处理”的事项，首版包括分配给我的未完成 Issue、待我批准及待我评审的 PR；“我创建的 Issue”“我发起的 PR”作为跟进入口，与待处理事项区分。按类别独立分页，提供已验证的状态、项目等筛选；每项显示标题、所属项目、事项类型、状态及更新时间，点击后进入可处理的详情页。数量必须注明统计口径，未取得完整统计时不将已加载条数标成全部待办数。
3. **通知**：集中查看选定项目范围内与当前用户相关的通知，展示未读状态、来源和更新时间；点击后进入对应 Issue、PR 或项目上下文。支持前台刷新和所选通知标记已读，明确覆盖范围；通知已读不代表相关待办已完成。
4. **我的**：个人资料、**我的项目、我的组织**、登录状态和设置。我的项目展示当前用户拥有或参与且有权访问的仓库，支持范围内检索；我的组织展示当前用户所属且有权访问的组织，进入后查看组织下可访问项目。项目详情承载 README、分支、标签、文件树、提交历史、Issue/PR 和 CI 入口；保留退出与授权撤销指引，多账号属于扩展项。

典型访问路径：

- 登录 → 动态首页 → 关联事项或项目详情；切换待办后处理需要自己完成的事项。
- 待办 → 待处理 Issue／PR → 查看讨论或 diff → 评论／批准 → 回读结果 → 更新待办列表。
- 通知 → 关联事项详情 → 处理事项 → 返回通知列表；已读与处理结果分别更新。
- 我的 → 我的项目 → 项目详情 → 文件／Issue／PR／流水线。
- 我的 → 我的组织 → 组织详情及可访问项目列表 → 项目详情。

本任务书中“项目”指代码仓库，不包含另行建设项目看板系统；“我的组织”首版为查看与导航，不包含组织创建、成员管理或权限管理。仓库详情可以从上述路径共用，不再设置独立仓库 Tab。动态首页限于已验证的本人及选定项目活动；推荐项目和全站发现不纳入首版。

### 3.3 首版范围

| 模块 | 必选交付 | 条件扩展，不作为默认验收项 |
| --- | --- | --- |
| 动态首页 | 本人及选定项目活动、来源与时间、分页和刷新、关联事项跳转、部分失败提示 | 更大范围的动态聚合 |
| 待办工作台 | 待我处理与我发起事项分类、独立分页、状态展示与筛选、详情处理入口、处理后回读和列表更新 | 多来源统一排序与混合分页、接口支持的更多个人待办类型 |
| 通知 | 选定项目通知、未读状态、关联事项跳转、所选通知批量标记已读 | 明确快照范围的“全部标记已读” |
| 我的与用户系统 | 单账号 PAT 登录、令牌有效性检查、个人资料、我的项目、我的组织、设置、退出和撤销指引 | OAuth 登录与刷新、多账号切换 |
| 项目与组织浏览 | 我的项目列表及范围内检索、我的组织列表、组织下可访问项目；项目详情、分支与标签、文件树、README、提交列表 | 更完整的项目统计、用户与 Issue 全局搜索、Star/Fork 写操作 |
| Issue 处理页 | 从动态／待办／通知／项目进入列表和详情，评论查看与发表、基础状态筛选 | 新建与指派、标签／里程碑筛选、关联 PR／分支展示、经验证的状态修改 |
| PR 处理页 | 从动态／待办／通知／项目进入列表和详情，按文件浏览基础 diff、普通评论、正式批准、状态展示 | 创建行内评论、完整评审线程交互、高级 diff 展示 |
| CI 上下文 | 从项目进入 v8 流水线运行列表与状态，提供有效网页入口；PR 关联运行仅在关联规则验证后展示 | 日志查看与下载、制品下载及后台传输 |
| 体验 | 手机窗口适配、深色模式、加载／空／错误状态、重试、四个入口与详情页的状态恢复 | 折叠屏和平板专项布局 |

条件扩展必须在对应接口验证通过、必选功能进度正常且不挤占最后两周缓冲的前提下纳入。OAuth、行内评论等验证通过也不自动成为必选项，应记录新增工作量与验收标准。

首版以 Issue/PR 覆盖已验证的个人待办，不因“工作台”定位而承诺 AtomGit 所有资源类型的处理能力。代码搜索、全站消息中心、正式 request-changes、AI 辅助和后台实时推送不纳入本期承诺。未确认的能力不得以不可用入口呈现；普通评论不能标示成正式拒绝审查或阻止合并。

### 任务安排与投入

以下以 280 人时为基线，允许根据前两周验证结果在 240–320 人时内调整；工作量均包含实现过程中的必要测试，不将测试集中留到最后。动态首页作为必选项，前两周结合事件接口与聚合规模复核工时；仍在既定总工时内安排，优先延后条件扩展，保护待办处理闭环与测试缓冲。

| 任务 | 基线工时 | 交付内容 |
| --- | ---: | --- |
| 1. 需求与技术验证 | 24 | 功能清单、用户流程、接口能力记录、关键验证原型 |
| 2. 架构与基础工程 | 36 | 工程骨架、导航、认证与网络基础、缓存设计、CI 基线 |
| 3. 核心模块实现与联调 | 140 | 首版必选功能、对应测试和真实接口验证 |
| 4. 集成测试与质量完善 | 40 | 真机回归、安全和性能检查、签名包 |
| 5. 文档与汇报 | 20 | 用户手册、设计与接口文档、测试报告、演示材料 |
| 6. 风险缓冲 | 20 | 接口变化、兼容性问题和验收缺陷修复 |
| **合计** | **280** | **缓冲工时不得预先分配给扩展功能** |

### 任务 1：需求与技术验证

前两周优先验证风险较高的链路，并确认现有工作台的可访问页面或设计材料，再完成移动端流程设计。

- **工作台调研**：以现有工作台为主要参考，梳理当前用户的待处理／跟进分类、状态、筛选和事项详情路径，形成“网页能力—接口依据—移动端页面—处理结果”的对照记录；完成动态、待办、通知、我的四入口原型。不把网页已有能力直接当作 OpenAPI 已支持能力。
- **待办与资源验证**：验证每类个人待办的查询、排序、去重和计数口径；确认我的项目、我的组织及组织下可访问项目的接口、成员关系、权限与分页。项目范围内筛选若只能在已加载数据中完成，应明确标示其范围。
- **认证验证**：以 PAT 完成用户信息读取；核实移动端 OAuth 应用注册条件、实际授权域名、回调 URI、公共客户端与 PKCE 支持、授权范围及刷新行为。OAuth 使用系统浏览器／Custom Tabs，验证 `state` 和回调。不将共享 `client_secret` 打包进 APK，也不复用其他应用的 OAuth 凭据。
- **协作验证**：在专用测试仓库验证普通评论、正式批准及结果回读；记录作者、评审者和只读用户的权限差异。
- **行内评论验证**：区分已有评论读取、回复和新建行内评论；分别核实端点、文件与行号定位、提交版本以及过期行为。仅有读取模型或普通评论成功不能作为新建行内评论的证据。
- **动态验证**：确认本人和选定项目事件接口的权限、类型、时间字段、去重标识、关联目标、排序与分页；确定聚合范围和请求上限。多来源第一页拼接不代表完整时间线，不将通知接口或待办列表直接冒充动态流。
- **通知验证**：确认选定项目范围的枚举方式、用户相关性、权限、分页、时间字段、去重标识及关联事项定位；验证通知已读请求编码与回读结果。确定首版选择项目数量上限与刷新策略，不将仓库事件流当作个人通知。
- **CI 验证**：确认 v8 的认证与权限，验证项目运行列表及状态字段；若显示 PR 关联运行，需验证提交或分支等关联依据，避免以无关的最新运行代替。日志与制品接口仅在扩展范围中评估。
- **范围冻结**：第 2 周末形成实际接口验证记录，确定最低支持 Android 版本、目标版本、依赖版本、设备测试矩阵和首版验收清单。关键必选接口若受阻，应说明原因并由导师确认替代方案，不得静默改成 Mock 演示后声称真实功能完成。

### 任务 2：架构与基础工程

- 实现“动态、待办、通知、我的”四入口的单 Activity 导航、主题、依赖注入、ViewModel 状态管理和 Repository 边界。统一事项与项目详情路由，使动态、待办、通知、我的项目和我的组织能够共用详情页，并保留来源页面的状态。
- 封装 v5/v8 客户端，使用 `Authorization: Bearer`，统一错误映射、超时、协程取消、响应体有界读取和脱敏。
- 分页大小、终止条件、响应数组或封装对象均逐接口确定，不假定所有接口统一支持默认 20、最大 100。
- 限流处理尊重有效的 `Retry-After`；缺失时使用有上限的退避和抖动。只对已确认可安全重试的操作自动重试，评论等写入发生不确定结果时先回读或交由用户处理，避免重复提交。
- OAuth 若纳入范围，同账号并发失效请求应合并刷新，避免刷新风暴；PAT 失效需重新登录，不伪造刷新能力。
- 凭据仅附加到受信任的 API 请求，不随 Markdown 外链、图片或下载重定向泄露。
- 明确缓存主键、权威来源、写后刷新、过期和退出清理；预留账号标识，多账号扩展时同时隔离数据库、图片缓存、分页与后台任务。
- 建立可重复构建与 CI：编译、静态检查、单元测试和可执行的 UI 回归检查。签名密钥与凭据不得进入代码仓库。

### 任务 3：核心功能模块实现与联调

按 3.3 节实现必选功能，优先打通“登录 → 动态首页 → 切换待办 → Issue/PR → 评论／批准 → 回读结果 → 更新待办列表”的完整流程，再完善通知和我的项目／组织入口。

- **动态首页**：维护本人及选定项目的事件流、分页游标或页码、来源和同步时间，支持去重、刷新及部分失败展示。可按来源分别分页；若采用混合时间线，应验证全局排序和继续加载策略。活动可关联时跳转到共用事项／项目详情；浏览动态不会自动完成待办或标记通知已读。
- **我的项目与组织**：实现个人项目、组织和组织项目列表的分页与访问权限处理；点击项目进入共用详情页，进入组织后仅展示当前用户可访问的项目。区分组织为空、无权限和请求失败，不把公共组织搜索结果当作我的组织。
- **项目内容与文档**：文件树逐层懒加载；文件和分支参数按接口要求编码；Markdown 正确处理相对链接和图片；不执行不可信脚本。大文件或不支持的格式展示明确说明及有效网页入口。
- **待办工作台**：分别调用已经验证的单条件查询。候选条件为 Issue 的 `created/assigned/all` 和 PR 的 `created_by_me/assigned_to_me/need_my_approve/need_my_review`，实际支持范围以验证记录为准；不能把 `all` 结果整体认作待我处理。区分待处理与我发起的跟进事项，去重键包含账号、仓库和资源标识，不将各来源第一页拼接结果称为完整全局列表。无完整计数接口时，使用明确的已加载数量或不显示总数。
- **处理结果一致性**：操作后回读事项、重新查询受影响的待办分类并刷新详情。评论成功不等于 Issue 已关闭，批准 PR 不等于 PR 已合并；只有服务端状态或待办查询结果确认变化后，才更新分类或移除事项。不同入口访问同一事项时保持状态一致，回读失败显示尚待同步，不虚构完成状态。
- **PR 与 diff**：优先单列、按文件加载，处理新增、删除、重命名、二进制和截断数据。为 diff 大小设置可测试的上限，并提供降级入口。批准前刷新状态与 head，若与用户查看版本不同则提示重新查看；请求后回读结果。客户端检查不替代服务端权限和并发控制。
- **通知与事项跳转**：限制选定项目范围与并发，维护各来源分页和同步状态，支持去重、缓存及部分失败展示。通知关联信息可解析时进入原生事项详情；资源已删除、无权限或无法定位时给出明确提示，必要时提供有效网页入口。列表不能因单个项目失败而错误显示“全部同步成功”；已读与待办处理状态分别管理。
- **已读操作**：必选项为所选通知批量标记已读，按仓库提交并报告部分失败。扩展“全部已读”时先确定范围和快照，分页收集目标 ID 后分批提交，不边翻未读页边修改导致漏项；仅对已确认处理的记录更新状态。
- **CI**：状态未知、无权限、无运行记录分别呈现。若增加日志和制品下载，应流式处理，提供大小限制、取消、失败恢复和适合目标 Android 版本的传输机制，不默认以常驻前台服务解决全部后台问题。
- **状态恢复**：四入口切换、详情返回、旋转及进程重建后恢复必要路由、动态来源、待办分类、项目／组织筛选、列表位置和草稿；明确 ViewModel 不等于跨进程持久化。

### 任务 4：集成测试、安全检查与交付打包

- 单元测试覆盖 ViewModel 的状态变化、Repository、实际存在的 UseCase，以及分页、错误映射、限流、取消和写入失败处理。重点验证待处理与跟进分类、数量口径、评论／批准后的待办刷新，以及通知已读不会改变事项业务状态。
- 使用 Fake Repository 检查 UI 状态；使用本地 Mock HTTP 服务和脱敏契约样例，通过 Android 实际使用的网络客户端验证方法、版本、路径、参数、请求编码和 DTO 解析。不能仅运行 Go CLI 的测试就认定 Android 客户端通过。
- 真实业务仓库仅做只读验证；专用测试账号与测试仓库在明确授权范围内进行评论、批准和标记已读等受控写入，写后回读。记录资源范围与清理方式，不对业务资源执行试验操作。
- Compose UI 测试覆盖登录后默认进入动态首页、动态刷新与事项跳转、四入口切换、通知定位事项、我的项目／组织到项目详情、评论／批准后返回列表及错误恢复；真机检查弱网、断网、令牌失效、权限不足、重复点击、旋转和进程重建。
- 检查 Token 加密、备份排除、日志脱敏、HTTPS、外链与重定向凭据隔离；若包含 OAuth，检查回调和刷新失败路径。
- 最低支持版本、一个主流中间版本及目标版本纳入测试矩阵，至少包含一台真实 Android 手机；记录设备、系统、构建版本和结果，不以“主流机型均支持”替代实测记录。
- 交付签名 Release APK，验证干净安装与基本升级流程。AAB 可作为附加发布产物，不能代替可直接安装的 APK。商店上架和新增服务端部署不属于默认交付。

### 任务 5：文档与项目总结

提交用户手册、架构与数据模型说明、实际接口调用清单、测试报告和总结 PPT 或技术博客。用户手册按“动态、待办、通知、我的”组织，说明动态浏览、事项处理、我的项目／组织访问和状态变化；设计文档包含现有工作台到移动端的流程映射。文档随实现更新，明确已交付、条件扩展和未实现内容；记录关键取舍、接口限制、已知问题与复现方式。

## 四、课题验收

### 4.1 12 周里程碑

| 时间 | 阶段成果 |
| --- | --- |
| 第 1–2 周 | 现有工作台调研、四入口原型、动态／个人待办与项目／组织接口验证、认证和验收范围确定 |
| 第 3–4 周 | 基础工程、PAT 登录、四入口导航、基础动态首页与待办分类列表可运行 |
| 第 5–7 周 | Issue/PR、普通评论、基础 diff、正式批准及处理后待办刷新完成联调 |
| 第 8–9 周 | 动态刷新与关联跳转、通知到事项跳转、我的项目／组织、项目内容浏览与 CI 状态完成联调 |
| 第 10 周 | 必选功能完成，集成回归；停止新增扩展范围 |
| 第 11–12 周 | 缺陷修复、真机验收、签名 APK、文档与汇报交付 |

若进度落后，优先停止条件扩展，保护核心协作流程和测试时间；涉及必选功能调整时同步更新验收清单与理由。

### 4.2 验收要求

- **代码**：源码提交至课题指定的 AtomGit 仓库，CI 通过；提供明确的依赖版本与构建步骤。架构边界清晰，网络层、Repository、ViewModel 和实际使用的 UseCase 有必要测试。
- **必选功能**：以第 2 周确认的清单验收。扩展项仅在明确纳入后验收；OAuth 未纳入时，PAT 登录即可满足认证验收。
- **工作台与导航**：底部按动态、待办、通知、我的排列四个入口，登录后默认进入动态首页；动态展示近期活动，待办优先展示需要当前用户处理的事项，我发起的事项作为跟进分类。我的项目、我的组织均能进入有权访问的项目，不设置独立仓库 Tab。
- **真实闭环**：在专用测试环境演示 PAT 登录 → 动态首页并刷新／查看关联活动 → 切换待办 → 查看事项／diff → 回复 Issue／批准 PR → 回读结果 → 返回待办查看正确分类与状态；演示通知 → 关联事项 → 返回并标记已读，以及我的 → 我的项目／我的组织 → 项目详情 → 文件与 CI 状态。批准流程使用作者与评审者不同的测试身份，不以自我评审演示。
- **异常行为**：演示断网、令牌失效、项目／组织权限不足及至少一个部分失败场景。写入失败不显示成功，不确定结果不自动重复提交；评论成功不错误标记 Issue 完成，通知已读不错误移除待办。
- **性能边界**：第 2 周记录代表性测试数据量和目标设备，第 10 周前检查列表、README 和 diff 是否存在阻塞、崩溃或明显内存问题；超过已声明范围时能够合理降级。
- **通知语义**：验收的是选定项目范围内通知的前台获取、事项跳转与已读处理，明确其覆盖范围；不要求后台即时到达，不声称覆盖全站消息中心。
- **交付物**：签名 APK、用户手册、设计文档、接口能力记录、测试报告、演示材料；报告区分离线验证、真实只读验证和受控写入验证。
- **完成时间**：领取课题后 3 个月内完成，最后两周包含在总周期内。
