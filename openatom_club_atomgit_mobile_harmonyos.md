# 俱乐部开源实习课题任务书

## 一、课题基本信息

- **课题名称**：AtomGit Mobile 鸿蒙原生客户端的设计与实现
- **指导导师**：慕冬亮
- **开发语言**：ArkTS；UI 使用 ArkUI，应用采用 Stage 模型
- **预计项目时长**：12 周，项目合计 240–320 人时，以 280 人时为排期基线；领取课题后 3 个月内完成开发与验收
- **团队规模**：2 名同学协作开发
- **投入说明**：两人平均分担时，每人每周约投入 10–14 小时；前两周根据团队经验、开发环境和接口验证结果复核投入。该预算仅覆盖 HarmonyOS 客户端，不包含另一 Android 课题的投入
- **难度等级**：高级
- **版本目标**：交付以了解项目动态和处理当前用户 AtomGit 待办为核心、能够在约定 HarmonyOS 真机上安装和使用的首版移动工作台；第 2 周确定验收范围，第 10 周完成首版功能，第 11–12 周用于测试、修复、文档与验收
- **课题背景信息介绍**：

AtomGit（atomgit.com）提供仓库托管、Issue 跟踪、合并请求（Pull Request）、流水线等开发协作能力。本课题以现有 AtomGit 工作台的任务组织方式为主要参考，以 OpenAPI 为数据来源，建设面向当前用户的鸿蒙原生移动工作台。应用以动态为默认首页，帮助用户了解本人及选定项目的近期活动，并通过待办发现需要自己处理的事项、进入上下文、完成评论或评审并确认结果；仓库、文件和流水线用于支撑待办处理，也可从“我的项目”“我的组织”进入浏览。GitHub Mobile 仅作为移动端细节交互的辅助参考。

平台资源接口以 `https://api.atomgit.com/api/v5` 为主，Actions 接口使用 `https://api.atomgit.com/api/v8`。具体路径、认证、权限、分页和响应字段必须逐端点验证，不能仅凭 API 版本或其他客户端的实现推断。

本课题面向能够运行所选 Stage 模型、ArkTS/ArkUI 应用的 HarmonyOS 手机。具体系统和 SDK API 版本在前两周根据真机与依赖确定；不以运行 Android APK 代替鸿蒙原生交付，也不默认覆盖所有历史 HarmonyOS 版本、OpenHarmony 设备、平板或 PC。

首版以应用直接访问已验证的 AtomGit 接口为基本方案。OAuth 若依赖保密的服务端凭据，或后续需要实时推送服务，应另行设计服务端并重新评估范围，不能默认包含在本课题工时内。

## 二、技能要求

- **具有移动 App 开发经验者优先，有 HarmonyOS、ArkTS/ArkUI 项目实践者更佳；具有 Android App 开发经验者同样优先。不将既有项目经验作为报名硬性条件。**
- 具备编程基础，能够使用或在项目初期掌握 ArkTS、ArkUI、DevEco Studio、Hvigor 和 Stage 应用模型，理解 UIAbility 与页面生命周期。
- 了解 ArkUI 状态管理与 MVVM，能够将页面展示、状态管理和数据访问分开，理解单向数据流和状态恢复。
- 具备 REST API 对接能力，能够封装异步请求、认证、分页、错误、超时与受控重试。
- 理解 PAT、OAuth 授权码、PKCE、浏览器回调，以及 HUKS 密钥管理和应用凭据安全存储的作用。
- 理解 Git 仓库、分支、提交、Issue、PR 和代码评审的基本语义。
- 能够使用版本控制、开展代码评审，并编写单元测试、接口契约测试、UI 测试和开发文档。

## 三、课题任务

### 3.1 技术选型与架构

采用 ArkTS、ArkUI 和 Stage 模型构建原生应用。以 MVVM 组织页面状态，通过 Repository 管理数据访问；复杂或可复用业务逻辑按需提取 UseCase，不强制所有接口经过额外领域层。

- **UI 与状态层**：ArkUI 页面负责展示和提交用户操作，ViewModel／状态对象负责加载、错误和业务状态。优先评估与目标 SDK 兼容的状态管理 V2，如 `@ComponentV2`、`@Local`、`@Param`、`@Event`、`@ObservedV2` 和 `@Trace`；固定状态管理方案和组件兼容边界，不机械混用 V1/V2。项目约定由状态持有者统一更新共享状态，子组件通过操作回调表达修改意图。状态机制参照[华为状态管理说明](https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/arkts-state-management-glossary)。
- **数据层**：Repository 统一协调 AtomGit API 与本地数据源，封装 DTO 映射、分页、平台差异和缓存失效。页面不直接调用网络或数据库。
- **可选业务层**：为多来源待办组合、跨页面复用的复杂业务规则提取 UseCase；简单查询可由 ViewModel 直接调用 Repository。
- **依赖管理**：通过构造参数或工厂注入客户端、Repository 和存储组件，便于测试替换。首版按功能与职责划分目录，只有复用或部署需求明确时才拆分 HAR/HSP，不为形式增加模块。
- **导航与视觉**：采用 ArkUI `Tabs`、`Navigation`、`NavDestination` 和 `NavPathStack` 等目标 SDK 支持的组件组织页面；底部仅设“动态、待办、通知、我的”四个入口，登录后默认进入动态首页。项目、组织、仓库与事项详情作为二级及更深层页面；从动态、待办或通知进入详情后返回原列表并保留筛选与滚动位置。基础布局与交互遵循鸿蒙设计规范。HDS 组件、高级材质和复杂动效仅在兼容性、性能和使用条件确认后作为增强项，不设为首版依赖。
- **网络层**：优先使用目标 SDK 提供的 Network Kit HTTP 能力，封装统一的 v5/v8 客户端。具体 Kit 导入路径和 API 以锁定 SDK 为准，不直接套用 Android 网络库或假定 Node.js 库可在 ArkTS 环境运行。
- **持久化**：Preferences 保存普通设置，relationalStore 按需缓存仓库和待办摘要；文件、diff、评论和流水线详情按需加载。每类数据明确权威来源、容量、有效期与退出清理规则。
- **凭据保护**：使用 HUKS 管理密钥，通过目标 SDK 支持的标准认证加密能力加密 Token，再将密文保存于应用私有存储。密钥不得与密文一起明文保存；明确备份／迁移策略以及密钥失效后的重新登录流程。Preferences 本身不提供加密，不能把“写入 Preferences”等同于安全存储，参照[官方存储能力比较](https://developer.huawei.com/consumer/cn/doc/doccenter-dev-faq/faqs-local-database-management-53)。
- **跨平台协作**：可与 Android 课题共享接口清单、脱敏契约样例、用户流程和验收用例；ArkTS 与 Kotlin 客户端分别实现、构建和测试，不假定原生 UI 或平台代码可直接复用。

### 3.2 信息架构

以现有 AtomGit 工作台的任务分类、状态展示和处理路径为主要参考，按移动端屏幕重新组织，具体页面按鸿蒙组件和交互规范实现。底部按 **动态、待办、通知、我的** 顺序设置 4 个入口，动态排在首位，是登录后的默认首页；待办承担具体事项处理。

1. **动态（默认首页）**：展示本人及选定项目的近期活动，帮助用户了解协作进展。首版支持来源展示、时间排序、分页、下拉刷新及关联事项跳转；具体事件类型以接口验证结果为准。动态用于了解发生了什么，需要自己处理的事项集中在待办，需阅读和标记已读的消息集中在通知，不混用三者的状态或数量。
2. **待办**：突出“需要我处理”的事项，首版包括分配给我的未完成 Issue、待我批准及待我评审的 PR；“我创建的 Issue”“我发起的 PR”作为跟进入口，与待处理事项区分。按类别独立分页，提供已验证的状态、项目等筛选；每项显示标题、所属项目、事项类型、状态及更新时间，点击后进入可处理的详情页。数量必须注明统计口径，未取得完整统计时不将已加载条数标成全部待办数。
3. **通知**：集中查看选定项目范围内与当前用户相关的通知，展示未读状态、来源和更新时间；点击后进入对应 Issue、PR 或项目上下文。支持前台刷新和所选通知标记已读，明确覆盖范围；通知已读不代表相关待办已完成。
4. **我的**：个人资料、**我的项目、我的组织**、登录状态和设置。我的项目展示当前用户拥有或参与且有权访问的仓库，支持范围内检索；我的组织展示当前用户所属且有权访问的组织，进入后查看组织下可访问项目。项目详情承载 README、分支、标签、文件树、提交历史、Issue/PR 和 CI 入口；保留退出与授权撤销指引，多账号属于扩展项。

典型访问路径：

- 登录 → 动态首页 → 关联事项或项目详情；切换待办后处理需要自己完成的事项。
- 待办 → 待处理 Issue／PR → 查看讨论或 diff → 评论／批准 → 回读结果 → 更新待办列表。
- 通知 → 关联事项详情 → 处理事项 → 返回通知列表；已读与处理结果分别更新。
- 我的 → 我的项目 → 项目详情 → 文件／Issue／PR／流水线。
- 我的 → 我的组织 → 组织详情及可访问项目列表 → 项目详情。

本任务书中“项目”指代码仓库，不包含另行建设项目看板系统；“我的组织”首版为查看与导航，不包含组织创建、成员管理或权限管理。仓库详情可以从上述路径共用，不再设置独立仓库 Tab。动态首页限于已验证的本人及选定项目活动；推荐项目和全站发现不纳入首版。

### 3.3 首版范围

| 模块 | 必选交付 | 条件扩展，不作为默认验收项 |
| --- | --- | --- |
| 动态首页 | 本人及选定项目活动、来源与时间、分页和刷新、关联事项跳转、部分失败提示 | 更大范围的动态聚合 |
| 待办工作台 | 待我处理与我发起事项分类、独立分页、状态展示与筛选、详情处理入口、处理后回读和列表更新 | 多来源统一排序与混合分页、接口支持的更多个人待办类型 |
| 通知 | 选定项目通知、未读状态、关联事项跳转、所选通知批量标记已读 | 明确快照范围的“全部标记已读”、桌面服务卡片 |
| 我的与用户系统 | 单账号 PAT 登录、令牌有效性检查、个人资料、我的项目、我的组织、设置、退出和撤销指引 | OAuth 登录与刷新、多账号切换 |
| 项目与组织浏览 | 我的项目列表及范围内检索、我的组织列表、组织下可访问项目；项目详情、分支与标签、文件树、README、提交列表 | 更完整的项目统计、用户与 Issue 全局搜索、Star/Fork 写操作 |
| Issue 处理页 | 从动态／待办／通知／项目进入列表和详情，评论查看与发表、基础状态筛选 | 新建与指派、标签／里程碑筛选、关联 PR／分支展示、经验证的状态修改 |
| PR 处理页 | 从动态／待办／通知／项目进入列表和详情，按文件浏览基础 diff、普通评论、正式批准、状态展示 | 创建行内评论、完整评审线程交互、高级 diff 展示 |
| CI 上下文 | 从项目进入 v8 流水线运行列表与状态，提供有效网页入口；PR 关联运行仅在关联规则验证后展示 | 日志查看与下载、制品下载及后台传输 |
| 体验 | 手机窗口适配、深浅色模式、加载／空／错误状态、重试、四个入口与详情页的状态恢复 | 折叠屏、平板和 PC 专项布局、高级材质与动效 |

条件扩展必须在对应接口与平台能力验证通过、必选功能进度正常且不挤占最后两周缓冲的前提下纳入。OAuth、行内评论等验证通过也不自动成为必选项，应记录新增工作量与验收标准。

首版以 Issue/PR 覆盖已验证的个人待办，不因“工作台”定位而承诺 AtomGit 所有资源类型的处理能力。代码搜索、全站消息中心、正式 request-changes、AI 辅助、后台实时推送、元服务形态和分布式协同不纳入本期承诺。未确认的能力不得以不可用入口呈现；普通评论不能标示成正式拒绝审查或阻止合并。桌面卡片、系统通知和元服务是不同能力，不能相互替代或当作后台保活方案。

### 3.4 双人协作

| 主责 | 主要工作 |
| --- | --- |
| 同学 A：基础能力与我的资源 | 工程骨架、登录与凭据、网络层、我的项目／组织、项目和文件浏览、README、CI 状态 |
| 同学 B：动态与协作 | 动态首页、待办工作台、Issue/PR、评论、diff、批准评审、处理后状态更新、通知及事项跳转 |
| 共同负责 | 接口验证、导航和公共组件、测试、文档、交叉评审与验收 |

前两周共同确定 Repository 接口、错误模型、页面状态、路由参数和测试样例。统一动态、待办、通知和我的项目／组织到共用详情页的路由及状态更新约定。之后在同一工程中按模块开发，每周至少集成一个可运行版本并交叉审查代码。主责划分不排除协作；公共层变更需同步接口和测试，避免最后集中拼接。沟通、评审和集成计入总工时。

### 任务安排与投入

以下以 280 人时为基线，允许根据前两周验证结果在 240–320 人时内调整。实现过程包含必要测试，不将测试集中留到最后；若学习或环境准备明显超出预计，应及时调整扩展范围并复核投入。动态首页作为必选项，前两周结合事件接口与聚合规模复核工时；仍在既定总工时内安排，优先延后条件扩展，保护待办处理闭环与测试缓冲。

| 任务 | 基线工时 | 交付内容 |
| --- | ---: | --- |
| 1. 需求与技术验证 | 24 | 功能清单、用户流程、接口能力记录、环境和关键验证原型 |
| 2. 架构与基础工程 | 36 | 工程骨架、导航、认证与网络基础、缓存设计、CI 基线 |
| 3. 核心模块实现与联调 | 140 | 首版必选功能、对应测试和真实接口验证 |
| 4. 集成测试与质量完善 | 40 | 真机回归、安全和性能检查、签名安装包 |
| 5. 文档与汇报 | 20 | 用户手册、设计与接口文档、测试报告、演示材料 |
| 6. 风险缓冲 | 20 | 接口变化、环境与兼容性问题、验收缺陷修复 |
| **合计** | **280** | **缓冲工时不得预先分配给扩展功能** |

### 任务 1：需求与技术验证

前两周优先验证风险较高的链路，并确认现有工作台的可访问页面或设计材料，再完成移动端流程设计。

- **工作台调研**：以现有工作台为主要参考，梳理当前用户的待处理／跟进分类、状态、筛选和事项详情路径，形成“网页能力—接口依据—移动端页面—处理结果”的对照记录；完成动态、待办、通知、我的四入口原型。不把网页已有能力直接当作 OpenAPI 已支持能力。
- **待办与资源验证**：验证每类个人待办的查询、排序、去重和计数口径；确认我的项目、我的组织及组织下可访问项目的接口、成员关系、权限与分页。项目范围内筛选若只能在已加载数据中完成，应明确标示其范围。
- **开发与安装验证**：确认 DevEco Studio、SDK、ArkTS、Hvigor 与依赖版本；确定最低支持 API、目标 API、真实设备型号与系统。落实签名所需开发者账号、证书、Profile 和设备条件，以最小 HAP 完成构建、签名和真机安装，不等到验收前处理环境问题。
- **认证验证**：以 PAT 完成用户资料读取；核实 AtomGit OAuth 应用注册条件、实际授权域名、公共客户端与 PKCE 支持、权限范围及刷新行为。OAuth 若纳入范围，采用系统浏览器授权，验证目标 SDK 的回调和应用唤起路径、冷／热启动处理以及 `state`；不内置共享 `client_secret`，不复用其他应用的 OAuth 凭据，也不把华为账号登录当作 AtomGit 授权。
- **协作验证**：在专用测试仓库验证普通评论、正式批准及结果回读，记录作者、评审者和只读用户的权限差异。
- **行内评论验证**：区分已有评论读取、回复和新建行内评论；分别核实端点、文件与行号定位、提交版本以及过期行为。读取模型或普通评论成功不能证明行内创建能力。
- **动态验证**：确认本人和选定项目事件接口的权限、类型、时间字段、去重标识、关联目标、排序与分页；确定聚合范围和请求上限。多来源第一页拼接不代表完整时间线，不将通知接口或待办列表直接冒充动态流。
- **通知验证**：确认选定项目范围的枚举方式、用户相关性、权限、分页、时间字段、去重标识及关联事项定位；验证通知已读请求编码与回读结果。确定首版选择项目数量上限与刷新策略，不将仓库事件流当作个人通知。
- **CI 验证**：确认 v8 的认证与权限，验证项目运行列表及状态字段；若显示 PR 关联运行，需验证提交或分支等关联依据，避免以无关的最新运行代替。日志与制品接口仅在扩展范围中评估。
- **能力记录**：为实际采用的端点记录功能、方法、完整路径、参数与编码、权限、分页、错误行为、脱敏样例、官方文档链接、参考客户端版本、验证日期与结果。区分仅文档、源码／Mock、真实只读和受控写入；不能将 CLI 已实现等同于鸿蒙客户端已验收。接口资料从 [AtomGit OpenAPI](https://docs.atomgit.com/docs/apis/) 和 [OAuth 文档](https://docs.atomgit.com/docs/apis/oauth/) 起查。
- **范围冻结**：第 2 周末确定首版验收清单、技术版本、测试矩阵和签名交付方式。关键必选接口若受阻，应说明原因并由导师确认替代方案，不得静默改成 Mock 演示后声称真实功能完成。

### 任务 2：架构与基础工程

- 实现 Stage 应用入口、“动态、待办、通知、我的”四入口导航、主题、ViewModel 状态管理和 Repository 边界。统一事项与项目详情路由，使动态、待办、通知、我的项目和我的组织能够共用详情页，并保留来源页面的状态。定义页面销毁、前后台切换时的请求与资源管理策略，不在 UI 构建方法中重复发起副作用。
- 封装 v5/v8 客户端，使用 `Authorization: Bearer`，统一错误映射、超时、取消与资源释放、响应体有界读取和脱敏。声明所需网络权限，正常校验 HTTPS 证书。
- 分页大小、终止条件、响应数组或封装对象逐接口确定，不假定所有接口统一支持默认 20、最大 100。
- 尊重有效的 `Retry-After`；缺失时使用有上限的退避和抖动。只对已确认可安全重试的操作自动重试；评论等写入结果不确定时先回读或交由用户处理，避免重复提交。
- OAuth 若纳入范围，同账号并发失效请求应合并刷新，避免刷新风暴；PAT 失效需重新登录，不伪造刷新能力。
- 凭据仅附加到受信任的 API 请求，不随 Markdown 外链、图片或下载重定向泄露。
- 缓存预留账号标识，多账号扩展时隔离数据库、图片缓存、分页与后台任务；退出时清除凭据和敏感缓存，避免迟到响应恢复旧登录态。
- 建立可重复构建与 CI，固定 SDK、Hvigor 和依赖版本；确认运行环境、工具获取条件与测试设备能力。没有模拟器／真机的 CI 环境先执行能够运行的检查，将设备测试放在可用环境中并保留结果，不把未执行的测试计为通过。

### 任务 3：核心模块实现与联调

按 3.3 节实现必选功能，优先打通“登录 → 动态首页 → 切换待办 → Issue/PR → 评论／批准 → 回读结果 → 更新待办列表”的完整流程，再完善通知和我的项目／组织入口。

- **动态首页**：维护本人及选定项目的事件流、分页游标或页码、来源和同步时间，支持去重、刷新及部分失败展示。可按来源分别分页；若采用混合时间线，应验证全局排序和继续加载策略。活动可关联时跳转到共用事项／项目详情；浏览动态不会自动完成待办或标记通知已读。
- **我的项目与组织**：实现个人项目、组织和组织项目列表的分页与访问权限处理；点击项目进入共用详情页，进入组织后仅展示当前用户可访问的项目。区分组织为空、无权限和请求失败，不把公共组织搜索结果当作我的组织。
- **项目内容与文档**：文件树逐层懒加载，路径和分支参数按接口要求编码。前两周验证 Markdown 渲染方案的 ArkTS／SDK 兼容性；正确处理相对链接、图片、代码块和大文档。若使用 Web 组件，禁止不可信脚本和不必要的桥接能力。大文件或不支持格式展示说明及有效网页入口。
- **待办工作台**：分别调用已经验证的单条件查询。候选条件为 Issue 的 `created/assigned/all` 和 PR 的 `created_by_me/assigned_to_me/need_my_approve/need_my_review`，实际支持范围以验证记录为准；不能把 `all` 结果整体认作待我处理。区分待处理与我发起的跟进事项，去重键包含账号、仓库和资源标识，不将各来源第一页拼接结果称为完整全局列表。无完整计数接口时，使用明确的已加载数量或不显示总数。
- **处理结果一致性**：操作后回读事项、重新查询受影响的待办分类并刷新详情。评论成功不等于 Issue 已关闭，批准 PR 不等于 PR 已合并；只有服务端状态或待办查询结果确认变化后，才更新分类或移除事项。不同入口访问同一事项时保持状态一致，回读失败显示尚待同步，不虚构完成状态。
- **PR 与 diff**：优先单列、按文件加载，处理新增、删除、重命名、二进制和截断数据；为 diff 大小设置可测试上限，提供降级入口。批准前刷新状态与 head，若与用户查看版本不同则提示重新查看；请求后回读结果。客户端检查不替代服务端权限和并发控制。
- **列表与性能**：根据所选状态管理方案采用兼容的懒加载和列表组件，数据分页由独立逻辑实现；不能把 LazyForEach 等 UI 渲染机制当作网络分页。对 diff 解析等耗时任务评估任务池／工作线程与数据传递成本，避免阻塞 UI。
- **通知与事项跳转**：限制选定项目范围与并发，维护各来源分页和同步状态，支持去重、缓存及部分失败展示。通知关联信息可解析时进入原生事项详情；资源已删除、无权限或无法定位时给出明确提示，必要时提供有效网页入口。列表不能因单个项目失败而错误显示“全部同步成功”；已读与待办处理状态分别管理。
- **已读操作**：所选通知按仓库批量提交，报告部分失败。扩展“全部已读”时先确定仓库范围和快照，分页收集目标 ID 后分批提交，不边翻未读页边修改导致漏项；仅对已确认处理的记录更新状态。
- **CI 与下载**：状态未知、无权限和无运行记录分别呈现。若增加下载，使用符合所选 SDK 和后台任务约束的机制，流式处理，提供大小限制、取消及失败恢复；不承诺应用进入后台后仍能任意持续执行。
- **状态恢复**：四入口切换、详情返回、窗口变化、前后台切换与进程重建后恢复必要路由、动态来源、待办分类、项目／组织筛选、列表位置和草稿；内存状态对象不能代替持久化。关闭页面或切换账号后防止旧异步响应更新错误界面。

### 任务 4：集成测试、安全检查与交付打包

- 使用与目标 SDK 兼容的 Hypium／单元测试能力，覆盖 ViewModel 状态、Repository、实际存在的 UseCase，以及分页、错误映射、限流、取消和写入失败处理。重点验证待处理与跟进分类、数量口径、评论／批准后的待办刷新，以及通知已读不会改变事项业务状态。
- 用 Fake Repository 检查页面状态；使用本地 Mock HTTP 服务和脱敏样例，通过应用实际使用的 ArkTS 网络客户端验证方法、版本、路径、参数、请求编码和 DTO 解析。仅运行 Go CLI 或 Android 端测试不能证明鸿蒙客户端正确。
- 使用目标 SDK 支持的 UI 测试工具覆盖登录后默认进入动态首页、动态刷新与事项跳转、四入口切换、通知定位事项、我的项目／组织到项目详情、评论／批准后返回列表及错误恢复；真机检查弱网、断网、令牌失效、权限不足、重复点击、前后台切换和进程重建。测试流程参考[华为开发者测试指南](https://developer.huawei.com/consumer/cn/testing/get-started/)。
- 真实业务仓库仅做只读验证；专用测试账号与测试仓库在明确授权范围内进行评论、批准和已读等受控写入，并回读验证。记录资源范围和清理方式，不对业务资源执行试验操作。
- 检查 Token 加密、密钥失效、备份／迁移行为、日志脱敏、HTTPS、外链与重定向凭据隔离；若包含 OAuth，检查回调与刷新失败路径。
- 至少包含一台与承诺版本匹配的 HarmonyOS 真机，记录设备、系统、SDK、构建版本和结果。若承诺多个 API／设备范围，应补齐对应测试；预览器效果不代替真机安装和功能验收。
- 交付能通过约定安装方式在验收设备运行的签名 HAP，以及安装说明、签名类型、Profile／设备限制和有效期说明；验收当天实际安装确认。若使用调试签名，应明确其范围，不称为可任意公开分发的发布包。签名要求参考[HarmonyOS 开发入门](https://developer.huawei.com/consumer/cn/develop-novice-guide/)。
- `.app` 发布产物和测试分发渠道可按需提供，不能代替真机可安装性验证。AppGallery 上架、AppTest 渠道开通或审核通过不作为默认验收条件；如另行要求，应单列账号、资质、材料和审核排期。私钥、密码、Token 和敏感签名配置不得提交至开源仓库。

### 任务 5：文档与项目总结

提交用户手册、架构与数据模型说明、实际接口调用清单、测试报告和总结 PPT 或技术博客。用户手册按“动态、待办、通知、我的”组织，说明动态浏览、事项处理、我的项目／组织访问和状态变化；设计文档包含现有工作台到移动端的流程映射。文档随实现更新，明确已交付、条件扩展和未实现内容；记录鸿蒙开发环境、签名安装步骤、关键取舍、接口限制、已知问题与复现方式。

## 四、课题验收

### 4.1 12 周里程碑

| 时间 | 阶段成果 |
| --- | --- |
| 第 1–2 周 | 现有工作台调研、四入口原型、动态／个人待办与项目／组织接口验证、签名真机安装、分工和验收范围确定 |
| 第 3–4 周 | 基础工程、PAT 登录、四入口导航、基础动态首页与待办分类列表可运行 |
| 第 5–7 周 | Issue/PR、普通评论、基础 diff、正式批准及处理后待办刷新完成联调 |
| 第 8–9 周 | 动态刷新与关联跳转、通知到事项跳转、我的项目／组织、项目内容浏览与 CI 状态完成联调 |
| 第 10 周 | 必选功能完成，集成回归；停止新增扩展范围 |
| 第 11–12 周 | 缺陷修复、真机验收、签名安装包、文档与汇报交付 |

若进度落后，优先停止条件扩展，保护核心协作流程和测试时间；涉及必选功能调整时，同步更新验收清单与理由。

### 4.2 验收要求

- **代码**：源码提交至课题指定的 AtomGit 开源仓库，约定 CI 检查通过；提供明确的依赖版本与构建步骤。架构边界清晰，网络层、Repository、ViewModel 和实际使用的 UseCase 有必要测试。
- **必选功能**：以第 2 周确认的清单验收。扩展项仅在明确纳入后验收；OAuth 未纳入时，PAT 登录即可满足认证验收。
- **工作台与导航**：底部按动态、待办、通知、我的排列四个入口，登录后默认进入动态首页；动态展示近期活动，待办优先展示需要当前用户处理的事项，我发起的事项作为跟进分类。我的项目、我的组织均能进入有权访问的项目，不设置独立仓库 Tab。
- **真实闭环**：在专用测试环境演示 PAT 登录 → 动态首页并刷新／查看关联活动 → 切换待办 → 查看事项／diff → 回复 Issue／批准 PR → 回读结果 → 返回待办查看正确分类与状态；演示通知 → 关联事项 → 返回并标记已读，以及我的 → 我的项目／我的组织 → 项目详情 → 文件与 CI 状态。批准流程使用作者与评审者不同的测试身份，不以自我评审演示。
- **异常行为**：演示断网、令牌失效、项目／组织权限不足及至少一个部分失败场景。写入失败不显示成功，不确定结果不自动重复提交；评论成功不错误标记 Issue 完成，通知已读不错误移除待办。
- **性能边界**：第 2 周记录代表性数据量和目标设备，第 10 周前检查列表、README 和 diff 是否存在阻塞、崩溃或明显内存问题；超过声明范围时能够合理降级。
- **通知与设备范围**：验收选定项目范围内通知的前台获取、事项跳转与已读处理，明确其覆盖范围；不要求后台即时到达，不声称覆盖全站消息中心。首版保证约定手机范围；卡片、平板、折叠屏和 PC 不作为默认验收条件。
- **交付物**：约定验收设备可安装运行的签名 HAP、安装说明、用户手册、设计文档、接口能力记录、测试报告和演示材料；报告区分离线验证、真实只读验证和受控写入验证，并注明未执行项目。
- **完成时间**：领取课题后 3 个月内完成，最后两周包含在总周期内。
